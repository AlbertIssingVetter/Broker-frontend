import React from "react";
import {withRouter} from 'react-router-dom';

class IRRWithdraw extends React.Component {

    render() {
        return (
            <>
                withdraw
            </>
        );
    }
}

export default withRouter(IRRWithdraw);
